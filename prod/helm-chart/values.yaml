# Default values for film-library-app.
namespace: prod

replicaCount: 1  # Replace '3' with the desired number of replicas

image:
  repository: itsvictorfy/final-proj
  tag: 2435c73
  pullPolicy: IfNotPresent
spec:
  replicaCount: 1

service:
  type: LoadBalancer
  port: 8080
database:
  host: postgresdb.database.svc.cluster.local
  database: "bW92aWVzREI="  # Base64-encoded value: "moviesDB"
  user: "cG9zdGdyZXM="    # Base64-encoded value: "postgres"
  password: "c3FsQWRtaW4="  # Base64-encoded value: "sqlAdmin"
